CVE-2024-49761

# this version of net-imap is included with the default ruby in the container, but not used
# as we install a newer version for the application
CVE-2025-43857

# this version of rexml is included with the default ruby in the container, but not used
# as we install a newer version for the application
CVE-2025-58767

# this version of uri l is included with the default ruby in the container, but not used
# as we install a newer version for the application
CVE-2025-61594

# devise, we monkey patched the user model; avoids major version upgrade as we will replace devise with SSO in the near future
CVE-2026-32700
# devise, solved with monkey patch to avoid major version upgrade
CVE-2026-40295

# vulnerable json version is included in ruby 4.0.2, gemfile specified a fixed version
CVE-2026-33210

# esbuild 0.28.0 (the latest) uses Go 1.26.1; these three CVEs require Go 1.26.2 which no
# esbuild release has shipped yet.
CVE-2026-32280 exp:2026-06-26
CVE-2026-32281 exp:2026-06-26
CVE-2026-32283 exp:2026-06-26

# we patched this in app, ignore residual alerts stale system deps
CVE-2026-42246

# Devise monkey patch added in config/initializers/devise_cve_2026_40295.rb
CVE-2026-40295
