- notice_types = {'success' => 'success', 'notice' => 'info', 'alert' => 'warning', 'error' => 'danger' }
- flash.each do |type, msg|
  %div.alert.alert-dismissible{:class => ["alert-#{notice_types[type]}", " #{type}"]}
    - if msg.is_a?(String)
      = msg.html_safe unless msg.blank?
    %a.close.icon-cross{href: '#', 'data-dismiss': 'alert', 'aria-label': 'close'}
.crumbs
  = yield :crumbs
