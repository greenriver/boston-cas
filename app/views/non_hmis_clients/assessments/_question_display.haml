- size ||= 'lg'
.c-question__display
  %span.d-block= label
  %span.c-metric{ class: "c-metric--#{size}"}
    - if value.is_a? Array
      - if value.any?
        = value.map(&:titleize).join(', ')
      - else
        N/A
    - else
      - if value.present?
        = value
      - else
        N/A
