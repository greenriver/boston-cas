Hello #{@contact.full_name},
\
= "CAS has a new housing recommendation with initial details for your review. You will receive another notification once the #{Translation.translate('case worker')} and client have had a chance to respond to the opportunity."
\
View details here:
#{notification_match_url @notification, @match}
\
= render 'location_details'
\
= render 'program_details'
\
- if @notification.decision.include_note?
  Notes:
  \
  #{@notification.decision.step_note}
\
\
\
= render 'email_footer'
