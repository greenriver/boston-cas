= render 'breadcrumbs'

.o-page
  .o-page__title
    - if ! @subprogram.closed?
      %h1 Edit Sub-Program
    - else
      %h1 CLOSED Sub-Program
    .ml-auto
      = render 'status_blocks'

  = render 'tab_navigation', active: 'edit'

  = render 'program_details'

  = render 'form'
